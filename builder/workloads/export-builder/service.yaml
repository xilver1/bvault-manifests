apiVersion: v1
kind: Service
metadata:
  name: export-builder
  namespace: bvault-prod
spec:
  type: ClusterIP
  ports:
  - port: 8081
    targetPort: 8081
    name: http
  selector:
    app: export-builder
