apiVersion: v1
kind: Service
metadata:
  name: yt-dlp-ingest
  namespace: bvault-production
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  selector:
    app: yt-dlp-ingest
